#include <stdio.h>
/**
 * main- printing lowercase alphabets
 * Retuen: returns 0 if program exits well
 */
int chat;
int main(void)
{
for (chat = 'a'; chat <= 'z'; chat++)
{
putchar(chat);
}
return (0);
}

#include <stdio.h>
/**
 * main- print decimal numbers
 * Return: returns 0 if program exits well
 */
int main(void)
{
int i;
for (i = 0 ; i < 10 ; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
